# CardForge Risk Register

Last updated: July 27, 2026

Every launch risk must be closed, explicitly accepted with a review date, or represented here until a tracked GitHub issue replaces it. “Implemented” is not provider or production proof.

| Area | Risk | Priority | Status | Evidence / next review |
| --- | --- | --- | --- | --- |
| Delivery gates | A change can bypass independent repository gates. | P0 | Closed; review scheduled | GitHub `Updates` ruleset is active on `main`: PRs, resolved threads, deletion/non-fast-forward blocks, and strict `verify` plus `public-smoke`; zero approvals is the documented solo-owner exception. Review August 15, 2026 or when a second trusted reviewer joins. |
| Production runtime | The canonical production deployment is unhealthy or has an unresolved error cluster. | P0 | Closed; monitor | Vercel deployment `dpl_9ncYWokxiUbiQ3epVY681GjMXKhw` is READY on production commit `8ed65a4f0bb0bf3252407b65c8a5f41a6949c1a2`. Five-route health [run 30008144709](https://github.com/pyralisxc/CardForge/actions/runs/30008144709) passed July 23; Vercel showed no grouped error in the preceding 24 hours and no retained 5xx cluster for the current deployment. Recheck after every production deployment. |
| Authenticated release proof | Free, paid, developer, owner, and paid project recovery evidence can be stale, skipped, or describe a retired auth entry path. | P0 | Closed; monitor | Stage 2 [run 29889963025](https://github.com/pyralisxc/CardForge/actions/runs/29889963025), job `88828243550`, passed the protected outcomes without skips on production commit `8ed65a4f0bb0bf3252407b65c8a5f41a6949c1a2`. Scheduled [run 30022834234](https://github.com/pyralisxc/CardForge/actions/runs/30022834234), job `89259701921`, repeated the protected outcomes without skips on July 23. Recheck after auth, billing, provider-domain, or entitlement changes. |
| Public Clerk control | The signed-out public header could lose its static `/sign-in` link, remain on a Connecting state, fail to reach an interactive dedicated Clerk form, or receive a failing bootstrap response. | P0 | Closed; monitor | The July 23 protected run passed `/` → visible `/sign-in` link → interactive Clerk form, captured Clerk bootstrap responses, and found no HTTP 400+ response or Clerk console error. Desktop/mobile/throttled acceptance remains a focused diagnostic after relevant UI or Clerk-domain changes, not a current closure blocker. |
| Security | Privileged Supabase functions are exposed to public roles. | P0 | Closed; monitor | July 15 live role checks denied privileged RPC execution to `anon` and `authenticated`, allowed `service_role`, and showed no warning/error security-advisor findings. Recheck after every migration. |
| Site-media migration provenance | Production and repository migration histories could disagree about the service-role grant required by owner homepage media. | P1 | Closed by PR #81 | Production migration `20260721220801_grant_site_media_service_role` is applied. Live checks confirmed only `service_role` has `SELECT`, `INSERT`, and `UPDATE`; `anon` and `authenticated` remain ungranted. PR #81 records the same migration in repository history without reapplying it. |
| Billing identity | Stripe account, receipt, and statement identity may not match public operator identity. | P0 | Closed; manual receipt delivery check remains | Direct Stripe review July 23 confirmed public business name **CardForge Studio**, website `https://cardforges.com`, and statement descriptor `CARDFORGE STUDIO`. The inspected Creator Pass payment has a receipt/invoice path but no receipt email had been sent; inspect one customer-facing receipt before treating email presentation as accepted. No Stripe mutation is authorized by this register. |
| Email identity | Resend sender and reply-to may not align with the support identity. | P1 | Closed; manual reply round trip remains | Resend verified `cardforges.com`; test email `09f0b42c-3b2f-4295-9aa2-55188b42e771` was delivered from `CardForge <support@cardforges.com>` with the configured reply-to. Complete one reply round trip before treating inbound support routing as accepted. |
| Support billing | A support payment could grant product access, or the support lane lacks live provider proof. | P0 | Closed; monitor | Stripe shows succeeded one-time and monthly support payments. Supabase processed the purpose-aware events, the support subscription has no Clerk entitlement mapping, and the owner console reports no product entitlement is expected. Recheck after billing-purpose or webhook changes. |
| Subscriber reconciliation | An existing Stripe subscriber may not map to the matching Clerk user. | P1 | Closed; monitor | July 23 provider review confirmed the Creator Pass subscription and paid access agree across Stripe, Clerk private metadata, Supabase, and the owner console. Reconcile again only after a relevant billing or identity change. |
| Operations | Business-signal failures lack assigned alert ownership. | P2 | Closed; monitor | Cameron owns launch-period response. GitHub Actions owns CI/smoke/health notification; Stripe plus the owner console and Supabase own billing diagnosis; Vercel owns runtime aggregation; Studio recovery UI owns persistence failures; owner contact history plus Resend own contact delivery. Recheck when a second operator or external alerting system is introduced. |
| Search | Search Console verification or sitemap submission could be incomplete. | P1 | Closed; monitor | Domain property verification is active. `https://cardforges.com/sitemap.xml` was submitted successfully, last read July 21, and reported 13 discovered pages. Recheck after domain or sitemap changes. |
| Canonical redirects | A common hostname may fail before it can redirect to the canonical apex. | P2 | Closed; monitor | Vercel serves valid TLS for `www.cardforges.com`; a July 23 live check returned permanent `308` to `https://cardforges.com/`. Apex HTTP, canonical metadata, robots, sitemap, and Open Graph URLs agree. Recheck after domain or canonical-host changes. |
| Data safety | Long artwork sessions can exceed browser quota or degrade recovery. | P1 | Accepted; monitor | IndexedDB persistence, limits, warnings, recovery snapshots, and import tests are released. July 23 live acceptance saved 72 cards, survived desktop refresh, and survived a 390×844 mobile reload with entitlement state intact. This is ordinary large-set recovery evidence, not an hours-long artwork/storage-pressure soak; Cameron accepts that residual risk for launch. Review August 15, 2026 or after a reported quota/recovery failure. |
| Dependencies | One accepted advisory remains without a compatible upstream fix. | P1 | Accepted | Three moderate package-path findings from one PostCSS advisory remain through `postcss`, `next`, and `@clerk/nextjs`; CardForge does not accept user-authored CSS for server-side serialization. Review by August 14, 2026 or when a patched compatible Next.js release is available. |
| Export correctness | Browser export does not provide CMYK/PDF/X or printer-specific duplex proof. | P1 | Accepted | Native browser export does not claim CMYK or PDF/X output; those remain external-prepress work until explicitly implemented and verified. Review with print-release scope. |
| Export acceptance | Implemented image, PNG-set, PDF, backs, and Tabletop Simulator paths could fail in a real browser or at large set sizes. | P2 | Accepted; manual proof remains | July 23 live paid-browser acceptance proved individual front/back downloads, a four-face PNG ZIP, PDF generation without an application error, a two-card TTS export, and 72-card persistence/reload. PR #85 bounded large-set browser memory, PR #86 restored the full 4K texture budget, and the subsequent 72-card production export completed with a 4090×3710 front texture. Export controls now disclose fixed raster pixels, separate PNG/PDF/TTS ownership, and offer efficient 10×7 or higher-detail 5×4 TTS sheets. Editor, preview, and export render from one canonical `CardPreview` surface; a six-element local comparison measured zero edit/preview position or size drift. Cameron accepts an actual Tabletop Simulator import and a visual check of the 5×4 package as the remaining manual launch evidence. Review August 15, 2026 or after export-renderer changes. |
| Analytics | Organic promotion could be unmeasured, collect more data than intended, or expose reporting credentials. | P1 | Closed; monitored | The owner-only Analytics Cockpit, normalized organic UTMs, and privacy-minimized adoption events are live. GA4 loads only after explicit visitor opt-in and the public collection flag; advertising storage, Google Signals, ad personalization, and Enhanced Measurement remain disabled. Google credentials are server-only and read-only, failed reports render unavailable, and CardForge does not duplicate raw events in Supabase. On August 11, 2026, the versioned privacy publication, provider access, Enhanced Measurement setting, key events, decline/allow behavior, sanitized tracked link, live Search Console report, and production deployment were verified. Re-run `docs/operations.md` after provider, privacy, or analytics-code changes. |
| Social publishing | A contributor or leaked provider credential could expose unapproved media, publish to the wrong channel, or bypass owner review. | P1 | Open; code hard-disabled | Campaign media now has canonical IDs, protected original/master storage, relational attachments, and idempotent public derivatives. Campaign/site scopes still default false behind `CARDFORGE_EXTENDED_CONTRIBUTIONS_ENABLED`; Buffer mutations require owner access, exact connected-channel validation, an optional production allowlist, and `CARDFORGE_BUFFER_PUBLISHING_ENABLED=true`. Focused code evidence is not provider proof. Keep both flags false until the final migration, legal publication, scoped production QA, channel allowlist, Buffer draft, harmless scheduled post, and durable status postflight in `docs/operations.md` are complete. |
| Architecture | Cross-feature cycles or retired root lanes return. | P2 | Closed; enforced | `npm run architecture:check` enforces zero violations with no exception baseline and rejects retired root `src/lib`, `src/store`, and `src/types` lanes. Recheck on every PR. |
